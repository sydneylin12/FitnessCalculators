<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sidlin.fitnessapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fitness Calculators"
        android:roundIcon="@mipmap/new_weight_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BmiActivity" />
        <activity android:name=".TdeeActivity" />
        <activity android:name=".MaxActivity" />
        <activity android:name=".MacroActivity" />
        <activity android:name=".TrackerActivity" />
        <activity android:name=".PopActivity" android:theme="@style/AppTheme.PopMe"/>
    </application>

</manifest>